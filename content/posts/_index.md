---
title: Posts
build:
  # /posts/ is not rendered. The home page already lists every post, and two
  # addresses answering one question is the thing that makes a reader wonder
  # which of them is the real one. The posts still live under content/posts/;
  # only the section's own page is dropped, and with it the second feed.
  render: never
  list: always
---
