/public/
/resources/
.hugo_build.lock

# The announcement, written here by hack/announcement.sh for review. It stays
# out of this repository until it publishes: a public history keeps a file that
# a later commit deletes, so committing a draft to preview it is publishing it.
/content/posts/introducing-opentacit.md
